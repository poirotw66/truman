# TODO

專案目標已從「單主角楚門式覺察實驗」轉向「箱庭小鎮：把 AI 村民放進去，看會發生什麼」。
最新方向是可殺人的武林箱庭（`scenarios/jianghu.py`，參考金庸《笑傲江湖》）。

## 待辦（依重要性）

### 4.5 兩個新問題（從 j2 跑出來的）
- [ ] **背水一戰幾乎不會觸發**：j2 全程 13 次動手，只有 1 次是帶傷者發動（j1 是 0/10）。
      機制寫了但用不到——LLM 角色帶傷就不出手了。要嘛接受它只是防呆，要嘛在 prompt
      裡讓帶傷的人知道自己「攻強守弱」，否則這條規則等於白寫。
- [ ] **義憤累積了卻沒有轉成行動**：儀琳 fury 封頂 4 到死都沒動手，劉正風 fury 2 也沒有。
      fury 目前只進骰子，沒進 prompt——角色不知道自己在憤怒。

### 5. 箱庭（hakoniwa）還沒實跑一整天
和平劇本至今只用 stub 乾跑過。梅姨的小聚引信訂在 tick 72（18:00），
要跑滿 96 tick 才看得到傍晚成不成局。

- [ ] 實跑 `hakoniwa` 96 tick（約 $1.2），看小聚有沒有傳開、傍晚咖啡館有沒有人

### 未做的取捨（記著，別忘了）
- **尋仇的 fork 韌性**：`add_runtime` 注入不進 WorldState。全程 replay 會重生（確定性），
  但若 checkpoint 剛好卡在死亡拍與觸發拍之間（12 拍對齊），fork 會漏掉那一則待發尋仇。
  跟 CLI 注入同樣的已知限制。真要補，改成同時寫一筆高 importance 記憶。
- **派系尋仇**：目前只認 kin 明線。五嶽劍派「同氣連枝」沒做成自動尋仇——費彬殺劉是
  左冷禪授意的「清理門戶」，全派自動尋仇會抹掉這層政治張力。留白是刻意的。

## 已完成（本輪）

- **回放接得起接力的 run，順便修掉 checkpoint 差一拍**：
  - `--run` 給多個時原本每段都當成 `[0, ∞)`，接力的 run tick 是重疊的，直接串會鬼影重疊。
    改成**切點由各 run 自己的 `fork` 事件推**（`at_tick`），不必人工指定。
  - **checkpoint 檔名比它描述的狀態晚一拍**：`engine.tick()` 先寫 snapshot（此時 `w.tick`
    還是 t）、再 `w.tick += 1`、然後才用**已經加一**的 `w.tick` 當檔名。所以 `t00012.json`
    記的是第 11 拍結束的世界。`build_frames.py` 兩處都用錯了 tick：對答案報 9 處不一致，
    而舊 run 的 BFS 校正會**真的把人物挪錯 3 格**（一個 MOVE_SPEED，畫面上就是瞬移）。
    改成建表時就以 `t-1` 為鍵，兩處一起正確。修完 j2 三段串起來 **0 處不一致**。
  - 驗證：97 幀、tick 0–96 無重複無斷層，兩個接力邊界的最大單拍位移 0 格與 3 格
    （都在 MOVE_SPEED 內，沒有瞬移），全片最大單拍位移也是 3 格。
  - `pillow` 加進 `[project.optional-dependencies]` 的 `art`/`all`——沒裝的話立繪靜靜不內嵌。

- **j2 全日重跑：「強者通吃」確實破了**（j2 + j2b + j2c，tick 0–96，共約 $0.75）。
  - **費彬第一個死**（10:00，曲洋所殺），而 j1/j1b 他殺四人、唯一生還。
    j2 原軌跡與 j2b 從 tick 24 重擲**兩次獨立擲骰都是同一個結局**，只差兩拍。不是運氣。
  - 真正起作用的是**「玻璃刀」那一半，不是背水那一半**：tick 23 曲洋先攻被擋，
    tick 24 費彬帶傷（wound 1）反擊——背水加成有給，他還是輸了；tick 25 曲洋補刀，
    費彬守勢被 -2×傷拖垮，一擊斃命。**是守勢懲罰殺了他，不是攻勢加成救了誰。**
  - 全日死三人（費彬/令狐沖/儀琳），存活三人（劉正風/田伯光/曲洋）。j1 只活一人。
  - **傍晚是安靜的**：j2c（17:40–亥時）零次動手，j1b 同時段六次、死三人。
  - **中文鎖有效**：真正的破口在 reflect 不在 thought/plan。j1b 的 insights+beliefs
    有 71 處夾英文（約一半），j2b 的 96 條 insights **0 處**。
    （`kind` 欄位的 speak/move_to/attack 是 schema enum，本來就是英文，不算。）
  - **尋仇那條線沒驗到**：劉正風根本沒死，沒有噩耗可報。曲洋是「先下手為強」不是尋仇。
    要驗尋仇得另外設計一個劉正風必死的局。

- **LLM 呼叫補上逾時與重試**（j2 第一次跑到 tick 26 就靜悄悄卡死，逼出來的）：
  `SimConfig.call_timeout=180` / `max_retries=3`；`BaseLLMClient._invoke_guarded()` 用
  `asyncio.wait_for` 強制逾時、指數退避重試、寫 `llm_retry` 事件；只重試暫時性錯誤
  （逾時/連線/429/5xx），schema 錯這種直接丟出不白花錢。Gemini client 另外吃
  `http_options={"timeout": ...}`（`wait_for` 只取消 task，這層才真的斷連線）。smoke 加 6 項。
  - **但那兩次停滯的真兇不是缺 timeout**：event loop 整個凍住時計時器根本不會跑
    （CPU 0%，SIGINT 解凍的瞬間積著的逾時才一次全部觸發）。改用
    `nohup ... & disown` **完全脫離 Claude Code 的背景 shell** 之後，j2c 26 拍
    零重試零停滯一路跑完。**長跑一律用 nohup，不要掛在 session 上。**

- **立繪重畫（俊男美女版）**：STYLE 加了一整段 BEAUTY 指示（理想化的主角級長相、平滑漸層的
  膚色、高細節眼睛含虹膜漸層與高光、髮絲高光、輪廓邊緣光、premium key art 的完成度），
  男角走「輪廓分明但精緻」的路線，女角走「明亮大眼、柔和五官、端莊」。
  POSE 也逐一改寫成有魅力的描述（費彬冷俊、令狐沖痞帥、田伯光危險的帥、曲洋白髮清癯的美）。
  - **改 STYLE 就整組重畫**（先前定的規矩）——六張全部 --force 重跑。
  - 修了兩個一致性問題：儀琳原本被畫成**西方修女的白頭紗**（恆山派是中國佛門），
    prompt 補上「中國佛門、剃度、海青、袈裟、念珠，明確禁止 wimple/veil/habit」；
    她的擬聲詞跑成黑字，於是把「白字＋粗描邊＋左上角」寫死進 STYLE，重畫一張對齊。
  - 儀琳十六歲，prompt 裡把「端莊、全覆蓋僧袍、不得有任何性化的姿勢或取景」寫成硬規定。
  - 舊版備份在 scratchpad（`portraits_v1`），要對照再說。

- **回放座標改吃 snapshot（真值）**：`replay/build_frames.py` 優先讀日誌裡的 `snapshot`
  事件（引擎每 tick 記全員位置／傷勢／生死），舊 run 沒有這個事件才退回 BFS 重建。
  snapshot 只記每 tick 結束時站哪，中間用 `walk_between()` 補一條最短路，讓回放仍是一格一格走。
  - 驗證：造了假 run 測單格與跨三格移動——逐格連續、終點與 snapshot 完全吻合；
    snapshot 與 checkpoint 對答案 **0 處不一致**（BFS 重建那條路是 13 格 drift）。
  - 順手加 `--run` / `--out`：`python replay/build_frames.py --run j2 --out j2_replay.html`，
    不必再改原始碼裡的 SOURCES。
- **拿掉沒有意義的循序暖機（跑得快一倍）**：`run_batch` 本來每個 tick 都先送一個呼叫、
  等它回來才並行其餘——用意是讓共同前綴先進快取。但 jianghu 的前綴約 2215 tokens，
  3.1 系列的快取門檻是 4096，**快取根本不會生效**，那一趟就是純浪費。
  現在只有「前綴真的進得了快取」才暖機，否則整批並行；每人自帶模型時還會依模型分組
  （拿 A 模型暖機救不了 B 模型的前綴）。模擬 2.5s 延遲實測：一個 tick 5.0s → 2.5s。
  smoke 加 7 項（含峰值併發驗證）。

- **每個人可以掛不同的模型／溫度／思考深度**（`AgentState.llm`，存在 cast.json 的 `llm` 欄位）：
  - `Call` 加 `model` / `temperature` / `thinking` 覆寫欄位；`base.call()` 改用 `c.model or cfg.models[tier]`。
  - **計價分桶**：自帶模型的呼叫記成 `層·模型`（例：`routine·gemini-3.5-flash`），
    否則會被按分層預設模型的價目計價，帳直接錯。報表兩個桶各自顯示自己的模型與成本。
  - Gemini：temperature 與 thinking_level 都吃得到。
    Anthropic：開了 extended thinking 不能給 temperature（API 會拒絕），這種情況安靜忽略溫度。
  - `cast.py` 驗證：溫度 0–2、thinking 在合法集合、模型不在價目表就警告（跑得動但帳不準）。
  - 工作室 UI：模型下拉（依 provider 分組，目錄從 `truman.config.PROVIDERS` 內嵌）、
    溫度滑桿（要勾「自訂溫度」才啟用，不勾就是跟隨預設）、思考深度、就地顯示每 MTok 價格；
    名單上有掛模型的人會標 🧠。
  - smoke 加 8 項；實跑 `--cast` 驗過 checkpoint 裡真的帶著設定。
  - **這是最乾淨的對照實驗手段**：同劇本同 seed，只換一個人的腦袋。

- **修「點人物卡死」**：燈箱的 class 取名 `.lb`，撞到角色卡裡的 `.sec .lb` 標籤，
  `position:fixed; inset:0` 把七個標籤變成全螢幕黑遮罩 → 一點角色整頁變黑點不動。
  改名 `.ptlb`。順手做的防呆：
  - 角色卡每 tick 只更新會變的欄位（原本整段 innerHTML 重建 = 每次重新解碼 78 KB 立繪）
  - 工作室的名單縮圖節點快取，不再每次點人重建六張 data URI 圖
  - 合成加 try/catch；連續 45 幀超過 24ms 自動關景深、再慢關泛光
  - 右上角加「✨ 特效」開關；燈箱可點擊或按任意鍵關閉
  實測：開著角色卡跑完 97 tick，零錯誤、每 tick 17ms、無殘留遮罩。

- **人物立繪（Gemini 影像模型產生）**：`art/gen_portraits.py` → `art/portraits/jianghu/*.png`，
  六張 896×1200。風格參考使用者指定的 JoJo 特徵：誇張姿態、大膽高彩度、時尚感服裝、
  硬朗面部輪廓、擬聲詞視覺化（每張背景一個大字：鏘／轟／嘩／顫／颯／錚）。
  - **一致性做法**：所有 prompt 共用同一段 `STYLE`（一字不改）＋ 先畫風格錨（劉正風），
    之後每張都把錨圖當**參考影像**餵進去要求對齊線條、網點、上色與背景處理。
    只靠文字描述對不齊，這是關鍵。
  - 姿態寫在 `POSE`，由人設決定：劉正風金盆前的告別揖、費彬指證的手勢、令狐沖舉葫蘆、
    儀琳抱念珠發抖、田伯光拔刀半蹲、曲洋抱琴回頭。
  - `art/embed_portraits.py` 縮到 480 寬轉 JPEG 壓成 data URI（六張約 512 KB），
    兩個 builder 自動內嵌，網頁仍是單檔離線可開。**沒有立繪就自動退回程式畫的像素立繪。**
  - 用在：回放頁的角色卡（點圖看大圖 lightbox，死者自動變灰）、片頭六人登場、
    名單頭像（臉部裁切）、人物工作室的預覽與名單。

- **HD-2D 畫面處理**（八方旅人那種味道，`replay/template.html`）：場景先畫進離屏 canvas，
  再疊三層合成——
  - **移軸景深**：上下模糊、中間一條清晰帶（這是 HD-2D 最好認的特徵）
  - **泛光**：燈籠／金盆／刀光登記成光源，畫成放射漸層再以 lighter 疊加，入夜特別明顯
  - **調色與暗角**：中央偏暖、四周壓冷壓暗
  舊機器可以把 `DOF` / `BLOOM` 設 0 關掉；瀏覽器不支援 canvas filter 會自動退回原本的畫法。
- **立繪（48×64 半身像）**：`drawBust()`，像素預算是地圖小人的十二倍，畫得出五官與表情。
  臉分層收出圓顱與尖下顎、斜肩、交領只露一條窄的白中衣、髮絲與髮髻／官帽／僧帽、
  長白鬚、疤、酒葫蘆、玉佩、袈裟。眉（平／壓眉／八字／挑眉）與嘴（抿／似笑非笑／小口／下撇）
  是可調參數，工作室裡有下拉選單。已歿會整張壓灰、閉眼。
  用在：回放頁的角色卡與片頭六人登場、人物工作室的預覽。
  **注意**：這是程式畫的像素立繪（Octopath 對話框那種），不是手繪／AI 生成的插畫——
  我沒有生圖能力，要真正的插畫得另外找圖源。

- **美術第二輪**（`web/pixelart.js`，兩個頁面同時受惠）：
  - **人物重畫**：兩趟畫法（先把每個方塊往外撐 0.45 格畫深色，再照原色畫一次）＝ 免費的一圈描邊，
    小人在地圖上不再糊成一坨。肩窄下擺寬的梯形身形、露出來的鞋、交領露出裡面的白中衣、
    腰帶帶扣、袖口與手、側面有鼻子的側臉、背面只有頭髮。
    隨身物 `acc`（酒葫蘆／玉佩／袈裟）讓六個人一眼認得出誰是誰。
  - **城外**：地圖往外多畫四格（`buildWorldCanvas` 的 margin），北面連綿的山＋山腳霧、
    西南水田（一格水一格旱、田埂）、東南與東面的林子、出東門的官道通到畫面外。
    以前城牆外是一片黑，看起來像地圖沒畫完。
  - **配色修正**：群玉院的粉紅壓成暗紅（原本是整張圖最搶眼的東西）、演武場青石轉暖、牆根長草。
  - **像素小圖示**取代 emoji（說話／動手／死／想通了／世界旁白），事件流、時間軸標記、圖例都換掉——
    emoji 每台機器長得不一樣，也跟像素美術打架。
  - **動態**：市集與酒樓的炊煙、風裡的落葉。
  - **片頭片尾**用整座城當底圖（片尾帶血色）。

- **人物工作室（開場之前的前端頁）**：`cast/build_editor.py` → `cast_editor.html`，
  離線可開、自帶資料。左邊名單（像素頭像）、中間表單、右邊地圖。
  - 改得動的：名字/id、身分門派、稱號、角色類型、武功（1–10）、家、**起始位置（直接點地圖）**、
    在意的人（kin，chip 勾選）、今日所求、江湖傳聞（公開人物表）、人設（大 textarea + 字數）、
    長相（衣色/滾邊/髮色/名牌色、頭上、鬚、兵器）＋即時像素預覽（四向 + 走路動畫）。
  - 可以新增／刪除人物；改 id 會自動更新別人 kin 裡的參照。
  - 「出門前檢查」即時列出問題（站在牆上、id 重複或格式錯、人設空白、kin 指到不存在的人、
    兩人同格、家不在地圖上）。改動暫存在 localStorage，關掉不會白改。
  - 匯入既有 cast.json（檔案或貼上）、複製 JSON、複製開跑指令、一鍵還原成劇本原設定。
- **`--cast` 讓設定檔真的能開跑**（`truman/cast.py` + CLI）：
  `--scenario jianghu run --cast cast/jianghu.json`。載入 → 驗證（一次列出全部問題，
  有問題就 exit 2，不燒額度）→ 套進世界；沒寫到的欄位沿用劇本預設，公開人物表也跟著換
  （拿掉的人不會還在世界區塊裡被介紹）。劇本 .py 不動。
  `cast/<劇本>.default.json` 是劇本原設定，可直接跑也方便 diff。smoke 加了 7 項檢查。
- **像素美術抽成共用模組** `web/pixelart.js`：回放頁與工作室都注入同一支，兩個頁面同一個世界。

- **回放畫面改成像素風遊戲**（取代原本的棋子版）：產生器搬進 repo `replay/`
  （`build_frames.py` + `template.html`，一支腳本直接產出根目錄的 `jianghu_replay.html`）。
  - 地圖全程式產生的像素圖磚：屋子畫成**掀了屋頂的室內**（地板/北牆/匾額/酒桌/屏風/香爐，
    南面留門），市集是布篷＋攤位檯面，演武場青石、城門包鐵大門、樹/井/紅氈/燈籠，
    建築往南投影。人在屋裡就看得見人，不是站在屋頂上。
  - 角色是像素小人：四向朝向、走路動畫、派別配色、髮髻/官帽/僧帽/白鬚、劍/刀/琴，
    帶傷有血點、重傷更明顯、死了是倒地＋血泊。另有十二個圍觀百姓，第一次見血後作鳥獸散。
  - 鏡頭：自動跟隨當刻最重要的事（動手 2.1×、對話 1.6×、平時看全員），動手時推近＋震屏＋
    紅光＋頓格，可切全景/縮放。氣泡永遠夾在畫面內（舊版會被切一半）。
  - 天色隨 tick 變（晨光→正午→黃昏→入夜），入夜燈籠亮起來。
  - 右側事件流（說話/動手/死亡/想通了/世界旁白，點一則跳過去）、時間軸上標 ⚔️💀 關鍵時刻、
    「上/下一場衝突」一鍵跳。
  - 角色卡：點小人或名單開卡，看人設、今日所求、武功條、此刻在想、說過的話（可跳）、
    關係（知音/交談過/動過手），開卡時地圖上畫關係連線。
  - 片頭六人登場（各一句今日所求）＋片尾結算（死亡名單/時刻/死於誰手、存活者、統計數字）。
  - 走位資料改成逐格 `steps`，回放是一格一格走而不是瞬移。
- **逐 tick snapshot 事件**（`engine.tick()`）：每 tick 記全員座標/傷勢/義憤/生死，一行 JSON。
  未來的 run 直接有走位資料，回放不必再重建（`replay/build_frames.py` 目前仍走 BFS 重放，
  改吃 snapshot 是之後可以簡化的地方）。
- **強者通吃三機制**（`_resolve_attack`）：先手 +2→+1；「背水一戰」帶傷者攻擊不受
  傷勢拖累、重傷更 +3（守勢仍 -2×傷，成玻璃刀）；「義憤」`fury` 欄位，親眼見殺
  +2（封頂 4），連續擊殺越來越難。見血後在場者一律 `action=None` 重新盤算，
  不會眼睜睜錯過命案。smoke：背水重傷者反傷強者 32/60、義憤上升、下手者自身不漲。
- **尋仇**（`_notify_kin`）：`AgentState.kin` 記在意的人；對方死了，用 director
  runtime inject 在下一拍把噩耗＋尋仇塞進親友眼前（觸發 needs_llm），人在城另一頭
  也傳得到。親眼看見的親友不重複報信。jianghu 目前只連劉正風↔曲洋知音一條線，
  其餘死亡靠義憤發酵。無親友者被殺不生尋仇注入（有測試守住）。
- **鎖中文**：MECHANICS 加「thought/plan 一律中文」、reflection_message 加
  「insights/beliefs 全中文」。都在共用區塊/罕跑的 reflect，成本可忽略。
- 世界引擎支援 `attack`：skill/wound/killed_by、引擎判定勝負、隨機源綁 (seed,tick,攻,守) 保 replay、死亡不可逆、擊殺兩拍不一擊斃命
- combat 徹底 gating：和平劇本的 schema enum 與世界區塊都不含動手
- prompt 拆層：SETTING/EXAMPLES 跟劇本，MECHANICS/TONE 共用
- 三本劇本：seahaven（原楚門，未動）、hakoniwa（和平箱庭）、jianghu（武林）
- report 新增：動手/死亡、誰跟誰說話、話題擴散、聚在一起
- 覺察偵測改成「沒有主角就整層不存在」
- 工程修正：max_output_tokens 900→1600、聽力射程 bug、駁回回饋進 observation、
  收工強制評審走 CLI 路徑、哨兵封頂 10.0

## 實跑紀錄

- `runs/j1` — jianghu tick 0–48（午後），$0.46，劉正風/田伯光死
- `runs/j1b` — fork j1 tick 48→96（亥時），$0.14，令狐沖/曲洋/儀琳死，只剩費彬
- `runs/j2` — 三機制後重跑 tick 0–26（tick 26 卡死），$0.35，費彬死於曲洋
- `runs/j2b` — fork j2 tick 24→70（被 session teardown 砍掉），$0.30，令狐沖/儀琳死於田伯光
- `runs/j2c` — fork j2b tick 70→96，$0.10，零死亡，劉正風與曲洋出城
  - **完整的 j2 軌跡要串 j2 → j2b → j2c 三段看**，中間兩段是接力不是反事實分支：
    `python replay/build_frames.py --run j2 --run j2b --run j2c --out j2_replay.html`
    切點由各 run 自己的 `fork` 事件推（j2 只取到 24，24–26 是被丟掉的舊分支）。
