# 複製成 .env（.env 不要進版控）。只需要填你實際會用的那一家。

# --- Anthropic（預設 provider）---
ANTHROPIC_API_KEY=sk-ant-...
# 或不用 API key，改跑 `ant auth login` 建立 profile，SDK 會自動讀取。

# --- Gemini（--provider gemini）---
GEMINI_API_KEY=....
# GOOGLE_API_KEY 也可以，SDK 兩個都認。
